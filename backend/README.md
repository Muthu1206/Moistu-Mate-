# Moistu‑Mate Backend

Dev:

```bash
cd backend
npm install
cp .env.example .env
# edit .env to set MONGODB_URI and JWT_SECRET
npm run dev
```

The server listens on `PORT` (default 4000). Endpoints follow the README and code comments.
